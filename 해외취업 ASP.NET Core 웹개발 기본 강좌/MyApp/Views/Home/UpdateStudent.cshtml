@model StudentTeacherViewModel
@{
    ViewBag.Title = "UpdateStudent";
}

<div class="container">
    <div class="d-flex justify-content-center">
        <div style="flex: 0 0 30%;">
            <h2>학생 수정</h2>
            <hr />
            <input type="hidden" id="test" value="test" />
            <form asp-antiforgery=true method="post">
                <div class="form-group">
                    <label>아이디 : </label>
                    <input type="number" class="form-controller" asp-for="@Model.Student.Id" />
                    <span asp-validation-for="@Model.Student.Id"></span>
                </div>
                <div class="form-group">
                    <label>이름 : </label>
                    <input type="text" class="form-controller" asp-for="@Model.Student.Name"/>
                    <span asp-validation-for="@Model.Student.Name"></span>
                </div>
                <div class="form-group">
                    <label>나이 : </label>
                    <input type="text" class="form-controller" asp-for="@Model.Student.Age"/>
                    <span asp-validation-for="@Model.Student.Age"></span>
                </div>
                <div class="form-group">
                    <label>국적 : </label>
                    <input type="text" class="form-controller" asp-for="@Model.Student.Country"/>
                    <span asp-validation-for="@Model.Student.Country"></span>
                </div>

                <div class="form-group mt-4">
                    <button type="submit" class="btn btn-primary">수정</button>
                    <button type="button" class="btn btn-primary" onclick="test()">test</button>
                </div>
            </form>
        </div>
        
    </div>
</div>